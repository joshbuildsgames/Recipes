{
  "name": "recipes-site",
  "private": true,
  "version": "1.0.0",
  "description": "Personal cookbook and cooking-technique reference, built as a static site with Eleventy.",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "start": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "markdown-it": "^14.1.0"
  }
}
